# Google Gemini (required for the agent; retrieval works without it)
GOOGLE_API_KEY=your-gemini-api-key-here

# Qdrant Cloud (optional — falls back to localhost:6333, then to embedded local mode)
QDRANT_URL=
QDRANT_API_KEY=

# Lyzr (optional — integration no-ops gracefully if unset)
LYZR_API_KEY=
